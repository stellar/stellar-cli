[package]
name = "preflight"
version = "0.1.0"

[lib]
crate-type = ["staticlib"]

[dependencies]
libc = "0.2.2"
soroban-env-host = {version = "0.0.9", features = ["vm"]}

[profile.release]
panic = 'unwind'
