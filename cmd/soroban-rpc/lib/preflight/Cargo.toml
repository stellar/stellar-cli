[package]
name = "preflight"
version = "0.7.1"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
libc = "0.2.2"
sha2 = "0.10.6"
soroban-env-host = { workspace = true, features = ["vm"] }
