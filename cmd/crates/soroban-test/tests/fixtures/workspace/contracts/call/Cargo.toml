[package]
name = "call"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = { version = "=21.7.2" }

[dev-dependencies]
soroban-sdk = { version = "=21.7.2", features = ["testutils"] }
