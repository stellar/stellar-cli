---
name: Docker

on:
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  docker:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - run: echo "Building and pushing Docker image..."
