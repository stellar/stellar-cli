name: Update CompletedSprint on Issue Closed

on:
  issues:
    types: [closed]
  pull_request:
    types: [closed]

jobs:
  update_completed_sprint:
    uses: paulbellamy/stellar-actions/.github/workflows/update-completed-sprint-on-issue-closed.yml@update-completed-sprint-on-issue-closed
    with:
      project_name: "Platform Scrum"
      field_name: "CompletedSprint"
